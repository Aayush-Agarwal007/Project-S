// Import Firebase scripts (auto-generated by Firebase SDK)
importScripts('https://www.gstatic.com/firebasejs/10.7.0/firebase-app.js');
importScripts('https://www.gstatic.com/firebasejs/10.7.0/firebase-messaging.js');

// Your Firebase config (same as in your main app)
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyCOFndrtqX-ZLDlbpT9SI2k7P78EllP9LI",
  authDomain: "project-s-8b77b.firebaseapp.com",
  projectId: "project-s-8b77b",
  storageBucket: "project-s-8b77b.firebasestorage.app",
  messagingSenderId: "915091110149",
  appId: "1:915091110149:web:8c7625e10c00f379819f6c",
  measurementId: "G-V8F7Z59X07"
};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);

// Retrieve Firebase Messaging instance
const messaging = firebase.messaging();

// Optional: Configure background notification handling
messaging.onBackgroundMessage((payload) => {
  console.log('Received background message:', payload);
  
  // Customize notification title and options
  const notificationTitle = payload.notification?.title || 'New Notification';
  const notificationOptions = {
    body: payload.notification?.body || 'You have a new message',
    icon: '/icons/notification-icon.png', // Adjust path to your icon
    badge: '/icons/badge.png',
    data: payload.data // Optional: include payload data
  };
  
  // Show the notification
  return self.registration.showNotification(
    notificationTitle,
    notificationOptions
  );
});
